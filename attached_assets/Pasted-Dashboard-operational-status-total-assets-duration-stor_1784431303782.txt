Dashboard (operational status — total assets, duration, storage, active jobs, recent activity) and an Insights page (AI-generated editorial analysis — themes, coverage gaps, story opportunities, featured people, top topics). React frontend, [your backend]. Keep them as two separate pages — do not merge them. Make the changes below in order. Don't break existing functionality; match the existing dark theme and card styling.
Part A — Shared data fixes (do these first, both pages depend on them):

Topic normalization. Topics are stored raw, so abraham_accords and abraham accords appear as two entries. Add a normalization function at the data layer: lowercase, replace underscores/hyphens with spaces, trim, collapse whitespace. Group by normalized key, sum counts, and display a clean human-readable label (title-case, but preserve known proper nouns like "Abraham Accords"). This must apply everywhere topics are shown, on both pages.
Reconcile duration numbers across pages. The Dashboard shows total playable duration; Insights shows total footage and speech-indexed hours. Make sure both pages read from the same source values so they never disagree. Expose three figures from the backend: totalDurationSec, speechIndexedSec, assetCount. Round for display: hours to 1 decimal (e.g. "49.6h") or "Xh Ym" format consistently — pick one and use it on both pages.
People: named vs. unidentified. Detect placeholder names with /^Person \d+$/i. Expose namedPeopleCount and unidentifiedPeopleCount separately from the backend. (Used by both pages below.)

Part B — Dashboard changes:

Add a "Searchable footage" figure to the stat cards or as a line under Total Duration: "Searchable: [speechIndexedSec as hours] of [totalDurationSec as hours]." This is the more important operational number — how much of the library is actually findable.
Add a "From your library" strip below the stat cards (above or beside Recent Activity). Pull the top 2–3 insight headlines from the Insights analysis — e.g. dominant theme, number of coverage gaps, number of story opportunities — and render each as a short teaser that links into the full Insights page (e.g. "5 story opportunities found →" links to /insights). Do not run new analysis here; read the already-generated insights result. If none exists yet, hide the strip.
Fix Recent Activity. Each card currently shows only a date, and all dates are identical, so it reads as an upload dump. Make each card (a) click through to that asset's detail/library view, and (b) show the actual event type where available ("Indexed", "Transcribed", "Added to project"), not just the date. If only upload date is available, label it "Added" so it's at least honest about what the event is.

Part C — Insights changes:

Update the People stat to show the split from A3: primary number = named count, secondary text = "X named · Y unidentified." In "Most Featured People," render unidentified people muted and add a small "Name" button next to each that [opens inline rename / calls your person-update endpoint].
Make people, topics, and named entities in findings clickable — each navigates to the existing Find/search view pre-filtered to that entity (e.g. /find?person=<id>, /find?topic=<normalizedKey>). Reuse the existing search route; add person/topic query-param handling if it doesn't exist. Don't build a new search page.
Turn "Story and Series Opportunities" from a prose paragraph into launchable cards. Each card shows: title (the angle), evidence (asset count, people involved, total speaking time), and a "Start story" button that creates a new project seeded with those assets and navigates to it (use the existing project-creation flow). [If an LLM step generates opportunities, change its output to structured JSON: { title, assetIds[], people[], totalDurationSec, rationale }. If computed in code, cluster assets by shared normalized topic + overlapping people, rank by total speaking time.] Show top 3–5.
Make Coverage Gaps concrete. Instead of prose ("limited content on cultural events"), quantify each gap: "Cultural events — 2 assets" etc., and make each gap clickable into the filtered Find view for that topic.

Constraints:

Round every displayed number sensibly; never show raw floats.
Keep the two pages visually distinct but consistent — Dashboard = operational/glanceable, Insights = analytical/considered.
If any endpoint I referenced doesn't exist, stub it with // TODO: wire to real endpoint and a sensible mock so the UI is testable.

Start with Part A (the shared data layer) and show me the normalization function, the reconciled duration values, and the named/unidentified split before moving to the page-specific UI. Then do Part B, then Part C.